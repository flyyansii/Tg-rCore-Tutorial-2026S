# rCore ch4 学习复盘与问答底稿

## 本章主题

第四章的核心是地址空间。前三章中，用户程序虽然已经能触发系统调用、被调度、被切换，但内存隔离还很弱。到了 ch4，内核开始为每个用户程序建立独立的地址空间，通过 RISC-V Sv39 页表把用户看到的虚拟地址翻译到真实物理页。

我对这一章的理解可以概括为：

```text
用户程序不再直接等于某块物理内存。
用户程序只看到自己的虚拟地址。
内核负责创建页表。
CPU 的 MMU 根据 satp 指向的页表自动完成地址翻译。
系统调用中，内核要先把用户传来的虚拟地址翻译成可访问的物理地址，才能读写用户缓冲区。
```

## 和前几章的递进关系

ch2 的重点是批处理和 Trap。用户程序通过 `ecall` 进入内核，内核处理完系统调用后再返回用户态。

ch3 的重点是多任务和分时。内核保存 `TaskContext` 和 `TrapContext`，让多个用户程序轮流运行。

ch4 的重点是内存隔离。每个进程有自己的 `AddressSpace`，也就是自己的页表。用户程序中的同一个虚拟地址，在不同进程里可以映射到不同物理页。

可以这样理解：

```text
ch2：能从用户态进内核态。
ch3：能在多个任务之间切换。
ch4：每个任务有自己的虚拟内存世界。
```

## 问答复盘

### 问题 1：为什么要引入虚拟地址？

我的初始理解是：为了把物理地址和程序使用的地址分开，避免不同程序互相覆盖。

修正后的答案：

虚拟地址的作用不仅是“防止覆盖”，更重要的是提供隔离和抽象。用户程序认为自己独占一片连续地址空间，但实际物理内存可以由内核分配到任意位置。页表负责记录“虚拟页号 VPN 到物理页号 PPN”的映射关系。这样每个进程都可以有自己的地址空间，进程之间不能随便访问对方的内存。

### 问题 2：Sv39 是什么？

我的初始理解是：它是 RISC-V 里面用来查页表的地址翻译方式。

修正后的答案：

Sv39 是 RISC-V 64 位架构的一种分页机制。它使用 39 位虚拟地址，并把虚拟地址拆成：

- 页内偏移 offset
- 第 0 级 VPN
- 第 1 级 VPN
- 第 2 级 VPN

CPU 的 MMU 会根据 `satp` 寄存器中保存的根页表物理页号，从高层页表一路查到低层页表，最后得到物理页号，再拼上页内偏移，得到真实物理地址。

### 问题 3：satp 是干什么的？

我的初始理解是：satp 保存页表的位置，切换 satp 就是切换地址空间。

修正后的答案：

这个理解是对的。`satp` 里包含分页模式和根页表物理页号。内核切换到某个用户进程运行前，需要让 CPU 使用这个进程自己的页表。此时写入 `satp`，MMU 后续访问用户虚拟地址时就会按该进程的页表进行翻译。

### 问题 4：为什么系统调用里不能直接用用户传进来的指针？

我的初始理解是：因为用户传的是虚拟地址，内核要根据页表找到真实物理地址。

修正后的答案：

这是 ch4 最重要的实践点。用户程序调用：

```rust
write(fd, buf, len)
```

这里的 `buf` 是用户虚拟地址。内核如果直接把它当作内核指针使用，就会访问错误位置，甚至导致 page fault。正确流程是：

```text
用户传入虚拟地址 buf
  -> 内核找到当前进程的 AddressSpace
  -> 调用 translate 检查权限并翻译
  -> 得到内核可访问的指针
  -> 内核再读写用户缓冲区
```

### 问题 5：mmap 和 munmap 在做什么？

我的初始理解是：mmap 是给用户程序分配一片虚拟内存，munmap 是取消映射。

修正后的答案：

`mmap` 的本质是修改当前进程的页表：在指定虚拟地址范围建立新的映射，并设置权限，例如可读、可写、可执行。`munmap` 则把某段虚拟地址范围对应的页表项清除，让这段虚拟地址失效。失效后如果用户程序继续访问，就应该触发 PageFault。

### 问题 6：sbrk 和堆有什么关系？

我的初始理解是：sbrk 是调整堆大小。

修正后的答案：

`sbrk` 调整的是进程的 program break，也就是用户堆的边界。增大 break 时，内核可能需要为新的虚拟页分配物理页并建立映射；缩小时，内核要撤销多余页的映射。ch4 的 `sbrk` 测试正是在验证堆扩展、堆收缩以及访问已释放区域会触发异常。

### 问题 7：为什么 ch4 Tetris 里也要用地址翻译？

我的初始理解是：用户程序把一帧游戏画面传给内核，内核需要从用户虚拟地址读取这个 frame。

修正后的答案：

这是把 ch4 原理用到游戏扩展里。用户态俄罗斯方块不能直接操作显卡，只能调用 `write(fd=3, frame)`。这个 `frame` 指针仍然是用户虚拟地址。内核中的 `graphics.rs` 必须先通过当前进程的页表把它翻译为内核可访问地址，再解析棋盘、分数、等级等数据，最后用 VirtIO-GPU 刷新 framebuffer。

## 本章需要记住的十个点

1. 虚拟地址是用户程序看到的地址，物理地址是真实内存地址。
2. 页表记录 VPN 到 PPN 的映射。
3. Sv39 使用三级页表。
4. `satp` 指向当前地址空间的根页表。
5. 切换进程时，本质上也要切换地址空间。
6. 用户指针不能在内核中直接解引用。
7. 系统调用读写用户缓冲区前必须进行地址翻译和权限检查。
8. `mmap` 是建立虚拟地址映射。
9. `munmap` 是撤销虚拟地址映射。
10. `sbrk` 是扩展或收缩用户堆。

## AI 协作复盘

本章学习中，我主要让 AI 做三类事情：

- 用通俗语言解释 Sv39、MMU、satp、页表等概念。
- 按模块梳理代码调用链，让抽象概念落到实际文件和函数上。
- 在实现 ch4 Tetris 时协助定位 bug，特别是页表开启后访问 VirtIO-GPU MMIO 地址导致 `LoadPageFault` 的问题。

这次最大的收获是：虚拟内存不是一张静态图，而是贯穿“加载程序、进入用户态、系统调用、驱动设备”的完整机制。只要用户态和内核态之间传递地址，就必须考虑地址空间。
